@{
    List<int> numbers = new List<int>();
    numbers = (List<int>)Session["numbers"];
}

@if (numbers != null)
{
    for (int i = 0; i < numbers.Count; i++)
    {
        <span class="number">@numbers[i]</span>
    }
}
